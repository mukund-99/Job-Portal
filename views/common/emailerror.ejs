<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Email Error | CareerConnect</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        :root {
            --primary: #2563eb;
            --primary-dark: #1d4ed8;
            --secondary: #f59e0b;
            --dark: #1e293b;
            --light: #f8fafc;
            --gray: #64748b;
            --success: #10b981;
            --error: #ef4444;
            --warning: #f59e0b;
        }

        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: var(--dark);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        .container {
            max-width: 500px;
            width: 100%;
            background: white;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2);
            text-align: center;
        }

        .error-header {
            background: linear-gradient(135deg, var(--error) 0%, #dc2626 100%);
            color: white;
            padding: 40px 30px;
        }

        .error-icon {
            font-size: 4rem;
            margin-bottom: 20px;
        }

        .error-header h1 {
            font-size: 2rem;
            margin-bottom: 10px;
        }

        .error-content {
            padding: 40px 30px;
        }

        .error-message {
            font-size: 1.1rem;
            color: var(--gray);
            margin-bottom: 30px;
            line-height: 1.6;
        }

        .error-details {
            background: #fef2f2;
            border: 1px solid #fecaca;
            border-radius: 8px;
            padding: 15px;
            margin-bottom: 25px;
            text-align: left;
        }

        .error-details h3 {
            color: var(--error);
            margin-bottom: 10px;
            font-size: 1rem;
        }

        .error-details p {
            color: var(--gray);
            font-size: 0.9rem;
            margin-bottom: 5px;
        }

        .solutions {
            text-align: left;
            margin-bottom: 30px;
        }

        .solutions h3 {
            color: var(--dark);
            margin-bottom: 15px;
            font-size: 1.1rem;
        }

        .solution-list {
            list-style: none;
        }

        .solution-list li {
            margin-bottom: 12px;
            display: flex;
            align-items: flex-start;
        }

        .solution-list i {
            color: var(--primary);
            margin-right: 10px;
            margin-top: 3px;
            flex-shrink: 0;
        }

        .action-buttons {
            display: flex;
            gap: 15px;
            justify-content: center;
            flex-wrap: wrap;
        }

        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 12px 24px;
            background: var(--primary);
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            text-decoration: none;
            gap: 8px;
        }

        .btn:hover {
            background: var(--primary-dark);
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(37, 99, 235, 0.3);
        }

        .btn-outline {
            background: transparent;
            color: var(--primary);
            border: 2px solid var(--primary);
        }

        .btn-outline:hover {
            background: #f0f5ff;
        }

        .btn-secondary {
            background: var(--secondary);
        }

        .btn-secondary:hover {
            background: #e59409;
        }

        .contact-support {
            margin-top: 25px;
            padding-top: 20px;
            border-top: 1px solid #e2e8f0;
            color: var(--gray);
            font-size: 0.9rem;
        }

        .contact-support a {
            color: var(--primary);
            text-decoration: none;
            font-weight: 600;
        }

        .contact-support a:hover {
            text-decoration: underline;
        }

        .error-code {
            background: var(--dark);
            color: white;
            padding: 10px;
            border-radius: 5px;
            font-family: monospace;
            font-size: 0.9rem;
            margin-top: 15px;
        }

        @media (max-width: 576px) {
            .container {
                margin: 10px;
            }
            
            .error-header {
                padding: 30px 20px;
            }
            
            .error-content {
                padding: 30px 20px;
            }
            
            .action-buttons {
                flex-direction: column;
            }
            
            .btn {
                width: 100%;
            }
        }

        /* Animation */
        @keyframes shake {
            0%, 100% { transform: translateX(0); }
            10%, 30%, 50%, 70%, 90% { transform: translateX(-5px); }
            20%, 40%, 60%, 80% { transform: translateX(5px); }
        }

        .shake {
            animation: shake 0.8s ease-in-out;
        }

        /* Loading animation for retry */
        .loading {
            display: inline-block;
            width: 20px;
            height: 20px;
            border: 3px solid rgba(255,255,255,.3);
            border-radius: 50%;
            border-top-color: #fff;
            animation: spin 1s ease-in-out infinite;
        }

        @keyframes spin {
            to { transform: rotate(360deg); }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="error-header">
            <div class="error-icon">
                <i class="fas fa-exclamation-triangle"></i>
            </div>
            <h1>Email Delivery Failed</h1>
            <p>We encountered an issue sending your email</p>
        </div>
        
        <div class="error-content">
            <div class="error-message">
                We're unable to send the email to the provided address. This could be due to an invalid email address, server issues, or network problems.
            </div>
            
            <div class="error-details">
                <h3><i class="fas fa-info-circle"></i> Error Message</h3>
                <p><strong>Suggestion:</strong> <span id="errorMessage">Use Another Email </span></p>


                <h3><i class="fas fa-info-circle"></i> Error Details</h3>
                <p><strong>Time:</strong> <span id="currentTime"><%= new Date().toLocaleString() %></span></p>
            </div>
            
            <div class="solutions">
                <h3>What you can do:</h3>
                <ul class="solution-list">
                    <li>
                        <i class="fas fa-check-circle"></i>
                        <span>Verify the email address is correct and try again</span>
                    </li>
                    <li>
                        <i class="fas fa-check-circle"></i>
                        <span>Check your internet connection</span>
                    </li>
                    <li>
                        <i class="fas fa-check-circle"></i>
                        <span>Ensure the email address is not blocked or invalid</span>
                    </li>
                    <li>
                        <i class="fas fa-check-circle"></i>
                        <span>Try using a different email address</span>
                    </li>
                    <li>
                        <i class="fas fa-check-circle"></i>
                        <span>Contact support if the problem persists</span>
                    </li>
                </ul>
            </div>
            
            <div class="action-buttons">
                <button class="btn btn-secondary" id="retryBtn">
                    <i class="fas fa-redo"></i>Try Again
                </button>
                <a href="/companies_register" class="btn btn-outline">
                    <i class="fas fa-arrow-left"></i> Back to Registration
                </a>
                <!-- <a href="/" class="btn btn-outline">
                    <i class="fas fa-home"></i> Go Home
                </a> -->
            </div>
            
            <!-- <div class="contact-support">
                <p>Still having issues? <a href="mailto:support@careerconnect.com">Contact our support team</a> or call us at <strong>1-800-HELP-NOW</strong></p>
            </div> -->
        </div>
    </div>

    <script>
        // DOM Elements
        const retryBtn = document.getElementById('retryBtn');
        const currentTimeEl = document.getElementById('currentTime');
        
        // Update current time
        function updateTime() {
            currentTimeEl.textContent = new Date().toLocaleString();
        }
        
        // Update time every minute
        setInterval(updateTime, 60000);
        
        // Retry button functionality
        retryBtn.addEventListener('click', function() {
            const originalText = retryBtn.innerHTML;
            
            // Show loading state
            retryBtn.innerHTML = '<div class="loading"></div> Retrying...';
            retryBtn.disabled = true;
            
            // Add shake animation to container
            document.querySelector('.container').classList.add('shake');
            
            // Simulate retry process (in real app, this would be an API call)
            setTimeout(() => {
                // Remove shake animation
                document.querySelector('.container').classList.remove('shake');
                
                // Restore button
                retryBtn.innerHTML = originalText;
                retryBtn.disabled = false;
                
                // Show success message (in real app, this would depend on the API response)
                alert('Retry attempted. Please check if the issue persists.');
                
                // In a real application, you would actually retry the email sending
                // window.location.reload(); // Or make an API call
            }, 2000);
        });
        
        // Auto-retry after 10 seconds (optional)
        setTimeout(() => {
            console.log('Auto-retry suggestion: Consider retrying the email sending');
        }, 10000);
        
        // Error type detection (you can customize this based on your error codes)
        function detectErrorType() {
            const error = '<%= typeof error !== "undefined" ? error : "" %>';
            const errorMessage = '<%= typeof errorMessage !== "undefined" ? errorMessage : "" %>';
            
            if (error.includes('5xx') || errorMessage.includes('server')) {
                return 'server';
            } else if (error.includes('4xx') || errorMessage.includes('invalid')) {
                return 'invalid_email';
            } else if (errorMessage.includes('network') || errorMessage.includes('timeout')) {
                return 'network';
            } else {
                return 'unknown';
            }
        }
        
        // Initialize error type detection
        document.addEventListener('DOMContentLoaded', function() {
            const errorType = detectErrorType();
            console.log('Detected error type:', errorType);
            
            // You could customize the UI based on error type
            switch(errorType) {
                case 'server':
                    document.querySelector('.error-message').innerHTML = 
                        'Our email servers are currently experiencing issues. We\'re working to resolve this quickly.';
                    break;
                case 'invalid_email':
                    document.querySelector('.error-message').innerHTML = 
                        'The email address appears to be invalid or doesn\'t exist. Please check the email format and try again.';
                    break;
                case 'network':
                    document.querySelector('.error-message').innerHTML = 
                        'Network connectivity issues detected. Please check your internet connection and try again.';
                    break;
            }
        });
    </script>
</body>
</html>